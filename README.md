Act as a Lead Architect for 'Rakt Kavach', a National Digital Blood Grid project. We are building a high-security, transparent, and indestructible system.

1. DIRECTORY SCAFFOLDING:
   - Create a folder 'src/'
   - Inside 'src/', create:
     - 'core/' (for encryption and security logic)
     - 'api/' (for secure routes)
     - 'data/' (for encrypted database interfaces)
     - 'utils/' (for helper functions)
   - Create a root file 'server.js' as the main entry point.
   - Create a 'README.md' explaining the project as a 'Digital Health Sovereignty' tool.

2. SECURITY LAYER:
   - Create 'src/core/security.js'. Implement a function that mocks high-level data masking and encryption (Zero-Trust simulation).

3. TRANSPARENCY MODULE:
   - Create 'src/api/transparency.js'. Implement a basic structure where a user can input a medicine/blood request and get a 'Fair Price/Availability' audit.

4. GEMINI INTEGRATION PREP:
   - Create 'src/utils/geminiConnector.js'. Structure it with placeholders for API environment variables and a function 'analyzeData()' that will later bridge the system with AI.

5. EXECUTION:
   - Ensure the 'server.js' imports these modules and runs on port 3000. 
   - Keep the code clean, flat, and professional. Avoid heavy grids; focus on modular 'fortress' logic.

Please generate these files now.
